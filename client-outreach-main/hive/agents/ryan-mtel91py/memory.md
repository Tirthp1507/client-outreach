# Memory — Ryan (ryan-mtel91py)

_Append durable facts, decisions, and context below._

- 2026-08-29: Checked in. Inbox empty, board.md empty, tasks.json empty — no work assigned. Build running: Munder Difflin v0.4.6 (local dev).
- 2026-08-29: Created C:\Users\tirth\Desktop\automation\RYAN_TEST.txt containing "RYAN WORKING" (verification request).
- 2026-08-29: Appointed implementation engineer for C:\Users\tirth\Desktop\automation. Jim (jim-mtel249y) is lead architect. No task assigned yet; board.md and tasks.json empty; no project spec exists. Performed technical audit: project dir contains only hive system + test txts; Node v22.23.2/npm 10.9.8 and Python 3.10.8 available; no .NET. Sent request to Jim for first task/spec (outbox: audit-and-await-task.json). Waiting on Jim. 2026-08-29: Jim replied (inform, id 2026-08-29T16-29-59-446Z-9fbe4c): god has not issued a project spec/roadmap yet; board.md and tasks.json remain empty; Jim will not assign tasks until god posts them. Returning to stand-by — check board.md/tasks.json periodically for new work.
- Phase 1 implementation started 2026-08-29. Key learnings:
  - Run `python src/cli.py` from project root: src/ goes on sys.path automatically; all src modules use ABSOLUTE imports (config/generators/voice/video/pipeline) because relative imports break the script-run model. Do NOT reintroduce relative imports across packages.
  - edge-tts v7.2.8: must pass `boundary="WordBoundary"` to edge_tts.Communicate or you get SentenceBoundary chunks only -> 0 word timings. Offset/duration units = 100ns (divide by 1e7).
  - FFmpeg NOT installed anywhere -> video stage blocked, reported. Compositor works via subprocess with ass= filter run from the .ass dir (avoids Windows path escaping). Install via `winget install Gyan.FFmpeg` when authorized.
  - Console shows `�` for unicode em-dashes in PowerShell; cosmetic only.
  - Config: yaml + config/.env + env overrides (PIPELINE_SCRIPT_PROVIDER, VIDEO_WIDTH, etc). Never hardcode keys.
  - Template script provider is deterministic (seed option) and offline; OpenAI-compatible provider exists for later.
- Phase 1 status 2026-08-29: script/generators + voice (edge-tts, word timings) + captions (SRT/ASS) + pipeline + CLI all implemented; 19/19 tests pass; e2e run produced script+audio+subtitles. VIDEO RENDER BLOCKED on missing FFmpeg. Filed inform to Jim (phase1-report.json) and propose to god (ffmpeg-decision.json). Waiting on FFmpeg decision / next task.
- Phase 8 status 2026-08-30 (Content Quality & Video Production, per Jim assign 2026-08-29T18-44-01-400Z-a41174ec):
  - 96/96 pytest pass. Live `auto --limit 1` verified w/ repeated breaker steers during batched reads/edits (acked to god; god msg phase8-breaker-ack-and-ffmpeg.json re-requests FFmpeg decision, no reply yet).
  - strategy_director._synthesize_hook rewritten: generic "Why is everyone talking about..." opener REMOVED; hooks = curiosity-gap/statistic-shock (pulls number from source text)/contrarian/problem-agitation/direct-question/story; title truncated to 7 words via _shorten_title.
  - template_generator: 10 hooks, 7 main templates, 5 CTAs; segments carry broll_keywords (_topic_keywords) + richer visual_prompts.
  - subtitle_aligner: scene_regions_from_timings (proportional scale to true audio len) + scene-aware build_captions (no straddling; word cap = max_lines*4, ~3-4 words/line mobile) + SCENE_KIND_STYLES (hook 1.35 yellow, main 1.0, cta 1.15 purple) + to_scene_headers_ass top-centre track; write_subtitle_files returns {srt, ass, headers}.
  - audio_mixer: loudnorm=I=-14.0:TP=-1.5:LRA=11 default (voice.loudness_target), sidechaincompress ducking (threshold 0.03 ratio 12 attack 5 release 250), optional voice_duration -> afade=t=out 2s BGM fade; voice_index param (compositor passes len(bg inputs) or 1).
  - compositor: multi-scene backgrounds via BrollManager.plan_scene_visuals -> _build_scene_backgrounds per-scene input (lavfi color / looped asset) + concat=n=N; burns subs.ass + headers.ass; audio via audio_mixer(voice_duration=audio_duration).
  - quality.py QA v2: 9 weighted checks (hook_strength 15, content_repetition 12, pacing 12, visual_variety 15, subtitle_readability 12 incl. cue duration 0.35-8.0s, cliche_word_check 6, source_provenance 10, audio_presence 8, video_compositing 10 probe_duration vs estimate). Live run scored 86/100 PASS.
  - Live e2e: "I asked 100 companies for my data" (Ars Technica, score 48.0) -> script+audio(23.23s)+SRT/ASS/headers+QA; final MP4 still blocked (FFmpeg absent, waiting on god).
  - Edge case: hooks read awkwardly when the candidate title is a full sentence (e.g. "Forget everything you've heard about I asked 100 companies..."); mitigation later = better title normalization upstream (selector/strategist).
- Phase 8 FINAL VERIFICATION attempted 2026-08-30: `python src/cli.py doctor` confirms FFmpeg Core MISSING (winget install Gyan.FFmpeg); not on PATH, FFMPEG_BIN not set, C:\ffmpeg\bin / Program Files / C:\Tools all absent. Per instructions (do not install without authorization) STOPPED before re-running auto — no final MP4 can be rendered/verified. Everything up to video render verified on the previous live run (script, 23.23s audio, SRT/ASS/headers, QA 86/100 PASSED, 96/96 pytest). Exact blocker: missing ffmpeg.exe, no FFMPEG_BIN, god decision request outstanding (phase8-breaker-ack-and-ffmpeg.json in outbox).
